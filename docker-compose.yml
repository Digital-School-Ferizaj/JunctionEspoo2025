version: "3.9"
services:
  api:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    depends_on:
      - ollama
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
  ollama:
    image: ghcr.io/ollama/ollama:latest
    ports:
      - "11434:11434"
    command: serve
